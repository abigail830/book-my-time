export interface EventData {
    allDay: boolean;
    id: string;
    groupId: string;
    start: string;
    end: string;
    title: string;
    url: string;

    backgroundColor: string;
    textColor: string;
}
